import Core from './core';
import { Intruder, CoreParam } from './types';
declare class Idmc extends Core {
    constructor(dataSource?: Array<Intruder>, param?: CoreParam);
}
export default Idmc;
